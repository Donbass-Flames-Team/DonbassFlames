UKR_ato_is_going = {
	allowed = {
		original_tag = UKR
	}

	enable = {
		OR = {
			has_war_with = DPR
			has_war_with = LPR
			has_war_with = KPR
			has_war_with = OPR
		}
		NOT = {
			has_global_flag = Ceasefire_is_active
		}
	}

	abort = {
		NOT = {
			has_war_with = DPR
			has_war_with = LPR
			has_war_with = KPR
			has_war_with = OPR
		}
	}

	ai_strategy = {
		type = garrison
		value = -500
	}
	
	ai_strategy = {
		type = front_unit_request
		tag = DPR
		value = 150
	}
	ai_strategy = {
		type = front_unit_request
		tag = NDR
		value = 75
	}
	ai_strategy = {
		type = front_unit_request
		tag = SDR
		value = 45
	}
	ai_strategy = {
		type = front_unit_request
		tag = LPR
		value = 130
	}

	ai_strategy = {
		type = front_unit_request
		tag = OPR
		value = 2
	}

	ai_strategy = {
		type = front_unit_request
		tag = KPR
		value = 4
	}

	ai_strategy = {
		type = front_control
		tag = SDR
		ordertype = front
		execution_type = balanced
		manual_attack = yes
		execute_order = yes	
	}

	ai_strategy = {
		type = front_control
		tag = DPR
		ordertype = front
		execution_type = balanced
		manual_attack = yes
		execute_order = yes	
	}

	ai_strategy = {
		type = front_control
		tag = LPR
		ordertype = front
		execution_type = careful
		manual_attack = yes
		execute_order = yes	
	}

	ai_strategy = {
		type = front_control
		tag = NDR
		ordertype = front
		execution_type = careful
		manual_attack = yes
		execute_order = yes	
	}


}

UKR_second_phase_of_ato = {
	allowed = {
		original_tag = UKR
	}

	enable = {
		has_global_flag = second_phase_of_ato
	}

	abort = {
		NOT = {
			has_global_flag = second_phase_of_ato
		}
	}

	ai_strategy = {
		type = garrison
		value = -200
	}
	ai_strategy = {
		type = front_unit_request
		tag = RUS
		value = -100
	}
	ai_strategy = {
		type = prepare_for_war
		id = DPR
		value = 100
	}
	ai_strategy = {
		type = prepare_for_war
		id = LPR
		value = 100
	}
}

UKR_counteroffensive = {
	allowed = {
		original_tag = UKR
	}

	enable = {
		has_country_flag = UKR_counteroffensive_is_happening
		NOT = {
			has_global_flag = Ceasefire_is_active
		}
	}

	abort = {
		NOT = {
			has_country_flag = UKR_counteroffensive_is_happening
		}
	}
	
	ai_strategy = {
		type = front_control
		tag = DPR
		ordertype = front
		execution_type = rush
		manual_attack = yes
	}

	ai_strategy = {
		type = front_control
		tag = LPR
		ordertype = front
		execution_type = rush
		manual_attack = yes
	}

	ai_strategy = {
		type = front_control
		tag = SDR
		ordertype = front
		execution_type = rush
		manual_attack = yes
	}

	ai_strategy = {
		type = theatre_distribution_demand_increase
		value = 5
		id = 237
	}
}

UKR_counteroffensive_in_kramatorsk_slavyansk = {
	allowed = {
		original_tag = UKR
	}

	enable = {
		has_country_flag = UKR_counteroffensive_in_kramatorsk_slavyansk
	}

	abort = {
		NOT = {
			has_country_flag = UKR_counteroffensive_in_kramatorsk_slavyansk
		}
	}
	
	ai_strategy = {
		type = front_control
		state = 291
		state = 318
		state = 327
		ratio = 0.1
		priority = 750
		ordertype = front
		execution_type = rush_weak
		execute_order = yes	
		manual_attack = yes
	}

	ai_strategy = {
		type = front_control
		tag = NDR
		ordertype = front
		execution_type = rush_weak
		manual_attack = yes
		execute_order = yes	
	}

	ai_strategy = {
		type = theatre_distribution_demand_increase
		value = 2
		id = 291
	}
}

UKR_TMR_hold_the_border = {
	allowed = {
		original_tag = UKR
	}

	enable = {
		always = yes
	}

	abort = {
		has_war_with = TMR
	}

	ai_strategy = {
		type = front_unit_request
		tag = TMR
		value = 10
	}
}

UKR_template_design = {
	allowed = { 
		original_tag = UKR 
	}
	enable = { 
		tag = UKR
	}
	abort = { 
		NOT = {
			tag = UKR
		}
	}

	ai_strategy = {
		type = template_prio
		id = infantry
		value = 45
	}

	ai_strategy = {
		type = template_prio
		id = modern_armor
		value = 40
	}

	ai_strategy = {
		type = template_prio
		id = mechanized
		value = 10
	}

	ai_strategy = {
		type = template_prio
		id = paratroopers
		value = 10
	}

	ai_strategy = {
		type = template_prio
		id = artillery
		value = 5
	}
}

UKR_ceasefire = {
	allowed = {
		original_tag = UKR
	}

	enable = {
		has_global_flag = Ceasefire_is_active
	}

	abort = {
		NOT = {
			has_global_flag = Ceasefire_is_active
		}
	}

	ai_strategy = {
		type = garrison
		value = -500
	}

	ai_strategy = {
		type = front_control
		tag = DPR
		ordertype = front
		execution_type = careful
		execute_order = yes	
	}

	ai_strategy = {
		type = front_control
		tag = LPR
		ordertype = front
		execution_type = careful
		execute_order = yes	
	}

	ai_strategy = {
		type = front_control
		tag = NDR
		ordertype = front
		execution_type = careful
		execute_order = yes	
	}


}