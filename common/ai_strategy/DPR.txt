DPR_first_phase_of_ato = {
	allowed = {
		original_tag = DPR
	}

	enable = {
		has_country_flag = DPR_first_phase_of_ato
	}

	abort = {
		NOT = {
			has_country_flag = DPR_first_phase_of_ato
		}
	}

	ai_strategy = {
		type = front_control
		tag = UKR
		ordertype = front
		execution_type = rush
		manual_attack = yes
		execute_order = yes
	}

	ai_strategy = {
		type = garrison
		value = -1500
	}
}

DPR_second_phase_of_ato = {
	allowed = {
		original_tag = DPR
	}

	enable = {
		has_global_flag = second_phase_of_ato
	}

	abort = {
		NOT = {
			has_global_flag = second_phase_of_ato
		}
	}

	ai_strategy = {
		type = front_control
		tag = UKR
		ordertype = front
		execution_type = rush
		manual_attack = yes
		execute_order = yes	
	}

	ai_strategy = {
		type = garrison
		value = -500
	}
}

DPR_hold_donetsk = {
	allowed = {
		original_tag = DPR
	}

	enable = {
		has_country_flag = DPR_first_phase_of_ato
	}

	abort = {
		NOT = {
			has_country_flag = DPR_first_phase_of_ato
		}
	}

	ai_strategy = {
		type = front_unit_request
		state = 432
		value = 35
		execute_order = yes	
	}
}

DPR_start_of_rebeltion = {
	allowed = {
		original_tag = DPR
	}

	enable = {
		has_war_with = UKR
		has_country_flag = DPR_first_phase_of_ato
	}

	abort = {
		NOT = {
			country_exists = NDR
		}
	}

	#Бот будет пытаться соединится с Гиркином

	ai_strategy = {
		type = front_control
		state = 426
		state = 401
		state = 48
		state = 381
		priority = 550
		ordertype = front
		execution_type = rush
		execute_order = yes	
		manual_attack = yes
	}

	ai_strategy = {
		type = ignore_army_incompetence
		value = 500
	}
}

DPR_debaltsevo_attack = {
	allowed = {
		original_tag = DPR
	}

	enable = {
		has_country_flag = Debaltsevo_battle
	}

	abort = {
		NOT = {
			has_country_flag = Debaltsevo_battle
		}
	}

	ai_strategy = {
		type = front_control
		state = 13
		state = 378
		state = 376
		ratio = 0.1
		priority = 530
		ordertype = front
		execution_type = rush
		manual_attack = yes
		execute_order = yes	
	}	
}

DPR_russian_forces_ilovaysk = {
	allowed = {
		original_tag = DPR
	}

	enable = {
		has_country_flag = DPR_forces_to_ilovaysk
		RUS = {
			has_completed_focus = RUS_open_invasion
		}
		date > 2014.7.7
	}

	abort = {
		NOT = {
			has_country_flag = DPR_forces_to_ilovaysk
		}
	}

	ai_strategy = {
		type = front_control
		state = 444
		state = 448
		state = 652
		ratio = 0.1
		priority = 500
		ordertype = front
		execution_type = rush_weak
		execute_order = yes	
		manual_attack = no
	}
}
