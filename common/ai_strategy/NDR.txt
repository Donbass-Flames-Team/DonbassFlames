NDR_first_phase_Of_ato = {
	allowed = {
		original_tag = NDR
	}

	enable = {
		has_country_flag = DPR_first_phase_of_ato
	}

	abort = {
		NOT = {
			has_country_flag = DPR_first_phase_of_ato
		}
	}
	
	ai_strategy = {
		type = front_control
		tag = UKR
		ordertype = front
		execution_type = careful
		manual_attack = yes
	}

	ai_strategy = {
		type = garrison
		value = -500
	}
}

NDR_hold_slavyansk = {
	allowed = {
		original_tag = NDR
	}

	enable = {
		has_country_flag = DPR_first_phase_of_ato
	}

	abort = {
		NOT = {
			has_country_flag = DPR_first_phase_of_ato
		}
	}
	
	ai_strategy = {
		type = front_unit_request
		tag = UKR
		state = 318  # SLAVYANSK AND KRAMATORSK
		value = 2000  
		execute_order = yes	
	}

	ai_strategy = {
		type = theatre_distribution_demand_increase
		value = 8
		id = 318
	}
}