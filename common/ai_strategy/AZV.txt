AZV_Counteroffensive_in_Mariupol = {
	allowed = {
		original_tag = AZV
	}

	enable = {
		OR = {
		    has_war_with = SDR
			has_war_with = DPR
		}
	}

	abort = {
		NOT = {
			has_war_with = SDR
			has_war_with = DPR
		}
	}

	ai_strategy = {
		type = front_control
		state = 526
		ratio = 0.1
		priority = 500
		ordertype = front
		execution_type = rush
		manual_attack = yes
	}

	ai_strategy = {
		type = front_control
		tag = SDR
		ordertype = front
		execution_type = rush
		manual_attack = yes
	}

	ai_strategy = {
		type = garrison
		value = -500
	}
}